<source>
  @type forward
  port 24224
  bind 0.0.0.0
</source>

<match **>
  @type loki
  url http://loki:3100
  flush_interval 2s
  buffer_chunk_limit 1m
  buffer_queue_limit 128
  <label>
    job fluentd
    host localhost
  </label>
</match>
